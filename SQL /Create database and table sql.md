````sql []
-- create a database
create database sharanya;

-- use database 
use sharanya;

-- create a table
create table class(
name varchar(100),
roll_number varchar(100),
Department varchar(100),
Phone_number int(10)
);

-- Display
select * from  class;

````
